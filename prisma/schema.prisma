// prisma/schema.prisma

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Song {
  id          String   @id @default(cuid())
  name        String
  instruments Json
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model Instrument {
  id       String @id @default(cuid())
  name     String
  category String // 日本語のカテゴリ名（"楽器類"など）をそのまま入れるためString
  order    Int

  // seed.tsの name_category: { name, category } に対応するために必須
  @@unique([name, category])
}

model Category {
  id        String   @id @default(cuid())
  name      String   @unique
  order     Int
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([order])
}

model InstrumentDependency {
  id              String @id @default(cuid())
  triggerCategory  String
  triggerName      String
  targetCategory   String
  targetName       String
  targetItems      Json

  @@unique([triggerCategory, triggerName, targetCategory, targetName])
  @@index([triggerCategory, triggerName])
  @@index([targetCategory, targetName])
}

enum InstrumentCategory {
  GAKKI
  KOMONO
  DRUMSET
  STANDCASE
  OTHERS
}